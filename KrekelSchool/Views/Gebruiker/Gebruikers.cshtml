@model IEnumerable<KrekelSchool.Models.ViewModels.GebruikerViewModel>

@{
    ViewBag.Title = "Gebruikers";
}

<h2>Gebruikers</h2>

<p>
    @Html.ActionLink("Create New Vrijwilliger", "AddVrijwilliger")
    @Html.ActionLink("Create New Medewerker", "AddMedewerker")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Uname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pswd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Naam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Uname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pswd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Naam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
           
            @Html.ActionLink("Delete", "Remove", new { id=item.Id })
        </td>
    </tr>
}

</table>
