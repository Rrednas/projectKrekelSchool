@using KrekelSchool.Models.Domain1
@using Microsoft.AspNet.Identity
@{
    ViewBag.Title = "AdminScreen";
}

<h2>@ViewBag.Title.</h2>
@using (Html.BeginForm("AdminScreen", "Item", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    <h4>@ViewBag.Message</h4>
    <hr />
    <div class="row">
        <div class="col-md-4 searchResult">
            <div class="row">
                <div class="col-md-8 search">
                    <textarea class="form-control formListSearch"></textarea>
                </div>
                <div class="col-md-8 btnSearchCol">
                    <a class="btn btn-default btnSearch" href="">Zoek</a>
                </div>
            </div>
            <label for="">Zoekresultaat:</label>
            @*<textarea disabled class="form-control formList"></textarea>*@
            @model IEnumerable<Leerling>

            <table class="">
                <tr>
                    <th>ID</th>
                    <th>Naam</th>
                    <th>Voornaam</th>
                    <th>Uitleningen</th>
                </tr>

                @foreach (var l in Model) {

                    <tr>
                        <td class="left"><@l.ID></td>
                        <td class="left"><@l.Naam></td>
                        <td class="left"><@l.Voornaam></td>
                        <td class="left"><@l.Uitleningen></td>
                    </tr>
                    };
            </table>

        </div>

        <div class="col-md-4 details">
            <label for="">Details zoekresultaat:</label>
            <textarea disabled class="form-control formList"></textarea>
            <div class="row">
                <div class="col-md-4 details">
                    <p><a class="btn btn-default btnMod" href="">Bewerk</a></p>
                </div>
                <div class="col-md-4 details">
                    <p><a class="btn btn-default btnRemove" href="">Verwijder</a></p>
                </div>
                <div class="col-md-4 details">
                    <p><a class="btn btn-default btnSave" href="">Opslaan</a></p>
                </div>
            </div>
        </div>
    </div>
}
